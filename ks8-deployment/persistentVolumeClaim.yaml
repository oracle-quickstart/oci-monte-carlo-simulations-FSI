apiVersion: v1
kind: PersistentVolumeClaim
metadata:
 name: splunk-fsspvc
spec:
 storageClassName: splunk-fss
 accessModes:
 - ReadWriteMany
 resources:
  requests:
 # Although storage is provided here it is not used for FSS file systems
    storage: 100Gi
 volumeName: splunk-fsspv
